_Giai đoạn 0: Khởi tạo dự án & Định hình công nghệ (Setup & Tech Stack)_
Trước tiên, hãy yêu cầu AI đọc hiểu CONTEXT.md (mà chúng ta đã bàn ở trên) và khởi tạo bộ khung chuẩn.
Prompt 0:"I want to build the 'Claims Intake Wizard' project as defined in the attached requirements. Please act as an Expert Frontend Engineer. Step 1: Initialize a Next.js (or React Vite) project using TypeScript.Step 2: Install Tailwind CSS, react-hook-form (for state management across steps), zod (for validation), lucide-react (for icons), and optionally set up shadcn/ui for quick accessible components.Step 3: Acknowledge when you are done and wait for my next command."

_Giai đoạn 1: Chuẩn bị Dữ liệu giả và Kiểu dữ liệu (Mock Data & Types)_
Theo đề bài, chúng ta cần mock data cho thành viên, người phụ thuộc, bệnh viện, và danh sách 100+ mã ICD-10.
Prompt 1:"Let's set up the data layer. Create a folder src/data and src/types.In types.ts, define the TypeScript interfaces for the entire multi-step form state, keeping in mind the conditional fields for Outpatient, Inpatient, and Dental. Create mockData.ts and generate:A mock user object (member name, policy number, member ID, DOB). A mock array of dependents. A mock array of 10 hospital/provider names. A mock array of at least 100 common ICD-10 codes (format: { code: string, description: string }).Do not build the UI yet. Just provide the types and data."

_Giai đoạn 2: Xây dựng "Vỏ bọc" Multi-step & Quản lý State (The Shell)Đây là bước quan trọng nhất để form không bị mất dữ liệu khi bấm Back/Next._
Prompt 2:"Now, build the multi-step form shell components.Create a WizardForm component using react-hook-form to manage the global state of all 5 steps. Build a Stepper UI component to show the user's progress (Steps 1 to 5). Implement Next and Back buttons. Ensure that navigating back and forth does NOT lose data (form state persists). For now, render placeholder components (e.g., <div>Step 1</div>) for the 5 steps. Ensure the shell is responsive and keyboard accessible."
_Giai đoạn 3: Triển khai Step 1 & Step 2 (Claim Type & Member Info)Bắt đầu đưa UI và Logic vào 2 bước đầu tiên._
Prompt 3:"Implement Step 1 and Step 2.Step 1: Render radio buttons or cards for 'Outpatient', 'Inpatient', and 'Dental'. Step 2: Pre-fill the form with the mock user data (make it editable). Add a question: 'Is this claim for a dependent?'. If yes, show a dropdown to select a dependent from the mock data.Ensure Zod validation is applied before allowing the user to click 'Next'."

_Giai đoạn 4: Triển khai Step 3 (Diagnosis & Treatment - Autocomplete)Đây là bước phức tạp vì có tính năng tìm kiếm ICD-10 và logic ngày tháng thay đổi theo Step 1._  
 Prompt 4:"Implement Step 3 (Diagnosis & Treatment) with conditional logic based on the Step 1 'Claim Type' selection. Create a free-text Diagnosis field. Build a performant Autocomplete Search component for the ICD-10 codes. It must filter the 100+ mock codes efficiently as the user types. Add Provider/Hospital input with suggestions from the mock list. Conditional Dates: If Outpatient/Dental, show a single Date picker. If Inpatient, show a Date Range picker. Conditional Inpatient Fields: If Inpatient, add fields for 'Admission reason' and auto-calculate 'Length of stay' based on the date range."

_Giai đoạn 5: Triển khai Step 4 (Document Upload)Xử lý logic upload giả lập và validation file._  
 Prompt 5:"Implement Step 4 (Document Upload).Read the Step 1 'Claim Type' to determine which documents to ask for.Outpatient: Receipt (required), Prescription (optional). Inpatient: Discharge summary (required), Itemized bill (required), Receipt (required). Dental: Receipt (required), Treatment plan (required ONLY if marked as major dental - add a checkbox for this). Implement an upload zone. Validate that files are only PDF, JPG, or PNG, and max 10MB each. Add a mock upload progress indicator (simulate a 0 to 100% progress over 1-2 seconds). Prevent the user from proceeding if required documents are missing."

_Giai đoạn 6: Triển khai Step 5 (Review, Submit & Polish)Bước cuối cùng để hoàn thiện dự án._  
 Prompt 6:"Implement Step 5 (Review & Submit).Render a clean Summary page showing all data collected from Steps 1-4. Add an 'Edit' button next to each section that navigates the user back to that specific step without losing data. Add a confirmation checkbox: 'I confirm this information is accurate'. Create the final 'Submit' button. Upon click, mock the submission by logging the entire form payload to the console and showing a Success Message/Alert. Finally, do a global review: Ensure the layout is responsive (mobile-friendly), fully tab-navigable, and pressing Enter proceeds to the next step."
